using Gtk 4.0;
using Adw 1;

template $StMovesPoolPage: Adw.PreferencesPage {
    Adw.PreferencesGroup {
        Stack stack {
            Spinner spinner {
              spinning: true;
            }
            ListBox pool_list {
                valign: start;
                hexpand: true;
                selection-mode: none;

                styles [
                    "boxed-list",
                ]
            }
        }
    }
}
